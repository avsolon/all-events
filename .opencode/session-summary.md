# Session summary

Last session: 2026-09-15 (сессии подряд — одна тема).

## Done
- Локально проверены фильтры (exclude_ended=279, is_upcoming=215, главная 6 событий все future), категория Акселлераторы (upsert-тесты), локальная миграция категорий, коммит+пуш фильтров/категории.
- TimePad: получен токен от пользователя, реализован авторизационный заголовок + фильтры (окно дат starts_at_min/max, сортировка, релевантность по category_tags, отсев записей/фейк-дат/test). Запущен полный прогон: 272 события → прод (TimePad 14), все saved. Коммит+пуш.
- Обновлены .opencode/context.md, tasks.md, decisions.md, infrastructure.md.

## Next
- Деплой на сервер (SSH недоступен — нужно пользователю): `git pull && docker compose build web && docker compose up -d web`.
- После деплоя: повторный прогон скрейпера локально (переназначение категорий в проде) и проверка проду.

## Known problems
- SSH root@138.124.70.3: Permission denied (publickey,password) — деплой только руками пользователя.
- Прод до деплоя на старом коде: последний прогон (272 события) не переназначил категории в проде.
- TimePad сырой API: ~69/100 событий — записи с плейсхолдер-датой 2050-01-01 (фильтруется).